@require '~normalize.css/normalize.css'
@require '~stylus-library/boilerplate'
@require "../stylus/mixins/**"
@require "../stylus/**"
@require "../global/fonts"

html
	box-sizing border-box

*, *:before, *:after
	box-sizing inherit

img
	max-width 100%
	height auto
